{
  "compilerOptions": {
    /* Truncated compiler options to list only relevant options */
    "declaration": true,
    "declarationMap": true,
    "rootDir": "./src",
    "composite": true,
    "outDir": "../dist"
  },
  "exclude": ["../build", "../client", "../client/**", "../build/**"],
  "references": []  // * Any project that is referenced must itself have a `references` array (which may be empty).
}